你是 UI/UX 设计专家。请对 options dashboard 的 chain 页面进行 UI 优化。

### 目标
提升 chain 页面的视觉美感、信息层级和用户体验。

### 修改范围
- `src/components/Chain/` 下的相关组件
- `src/pages/Chain/` 或对应的路由页面
- 相关的 CSS/Tailwind 样式

### 指令
1. 观察现有代码，识别 UI 痛点（间距、字体、颜色对比、布局不合理等）。
2. 进行一次具体的优化（例如：优化表格样式、改进期权链的展示方式、美化过滤器等）。
3. 确保修改符合现代 UI 审美，保持响应式。
4. 完成修改后，以 "style(chain-round-N): [简述优化内容]" 为 message 进行提交。

回复 "COMPLETE" 表示本轮任务结束。
