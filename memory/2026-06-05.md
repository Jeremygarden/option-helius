# 今日记忆 2026-06-05

## 重要决策和方向确认
- **数据源调整**：确认接入 `yfinance` 作为期权链的核心数据源（P0优先级），因其免费且能快速支撑起前端 Tab。
- **持仓接入计划**：计划通过 `IBKR Flex Query` 接入真实账户持仓数据（P1优先级）。
- **同步推进策略**：前端容器部署与后端真实数据接入同步进行。

## 技术实现进展
- **后端修复**：
    - 识别到 `backend` 启动失败的原因为缺少 `httpx` 和 `yfinance` 依赖。
    - 计划在 `pyproject.toml` 中补全依赖并重新构建。
    - 计划新建 `backend/app/services/ibkr_flex.py` 和 `ibkr_parser.py` 用于处理 IBKR 数据。
- **前端修复**：
    - 解决了 `BacktestTable.tsx` 缺少 `'use client'` 指令导致的崩溃问题。
    - 修复了 `data` prop 的可选性处理。
- **基础设施**：
    - 完成了服务器 Docker 环境的安装与磁盘空间清理（释放 6.6G）。
    - 确认 Redis 和 TimescaleDB 正常运行。

## 讨论结果和待办事项
- **待办 (TODO)**：
    - [ ] 修复 `backend` 依赖并重启服务。
    - [ ] JJ 提供 IBKR 的 `Flex Token` 和 `Query ID`。
    - [ ] 开发 `ibkr_flex.py` 服务层，替换目前的 Mock 数据。
    - [ ] 验证前端 `chain` tab 在真实数据下的渲染情况。
- **优先级**：
    1. yfinance 期权链接入。
    2. Flex Query 账户数据接入。
    3. 前端界面各 Tab 依次上线可用。

## 数据源和API确认
- **yfinance**：用于拉取市场期权链、IV、Greeks（模拟/基础）、OI/Vol。
- **IBKR Flex Query**：用于拉取个人持仓、历史成交、行权记录。
- **API URL**：
    - Request: `https://gdcdyn.interactivebrokers.com/.../FlexStatementService.SendRequest`
    - Fetch: `https://gdcdyn.interactivebrokers.com/.../FlexStatementService.GetStatement`
