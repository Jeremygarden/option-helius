# 2026-05-31 option-helius 工作记忆

### 重要决策与方向确认
- **模型路由优化**：确定了以 `gemini-3-flash-preview` 为日常默认模型，`claude-opus-4.6` 处理深度推理与编程任务的策略。
- **Cron 稳定性保障**：因模型下架导致 cron 连续失败 8 天。决策：cron 任务强制绑定稳定模型，并在 `HEARTBEAT.md` 中增加 cron 健康检查，失败时需主动告警。
- **期权精选逻辑**：从“全策略无差别打分”转向“环境优先筛选”模式。先根据标的价格走势、IV Rank、技术指标过滤不适用策略，再对高质量候选池进行打分。

### 技术实现进展
- **后端引擎重构**：
    - `options_scanner.py`：实现基于 yfinance 的实时数据抓取、方向判断（均线/动量/RSI/关键位）及策略适用性过滤。
    - `strategy_scorer.py`：实现包含 Sell Put, Covered Call, Spread, Iron Condor 在内的六大策略打分引擎。
- **前端页面重构**：
    - `/picks` 页面：对照参考截图完成全量重写。新增标的搜索扫描、快捷 Ticker、最终方案卡片（含入场触发与风控）、双图表展示（K线与分时/VWAP）以及 2x4 期权候选池网格。
- **自动化运维**：修复并重新配置了 `options-daily-memory` 任务，确保每日北京时间 23:59 自动执行工作总结。

### 待办事项与下一步计划
- **真实数据对接**：将前端 Mock 数据全面替换为 `options_scanner.py` 返回的真实 yfinance 数据。
- **代码库全面审计**：启动 subagent 使用 `claude-opus-4.6` 对现有代码库进行完整扫描，重构早期冲动编写的漏洞和不一致设计。
- **哨兵系统集成**：将 MOVE、HY OAS、收益率曲线三大哨兵指标集成到宏观预警面板核心位置。
