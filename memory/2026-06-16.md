# Option-Helius 工作记忆 - 2026-06-16

## 1. 重要决策和方向确认
- **环境适配**：确立了在受限网络环境下通过配置国内镜像源（如清华源）和代理（v2rayN）来保障 Docker 构建流程的方案。
- **身份一致性**：决定并执行了对项目历史 commit 的清理，统一使用 `mingxlee23@gmail.com` / `jeremygarden` 身份，以确保 GitHub Contribution Graph 正确统计。

## 2. 技术实现进展
- **Docker 构建优化**：
    - 在 `backend/Dockerfile` 中集成了 `uv` 包管理器。
    - 针对 `pip install uv` 和 `uv sync` 阶段均配置了清华大学 PyPI 镜像源。
    - 修复了 Docker 构建时因网络抖动导致的镜像层损坏（size validation failed）问题。
- **Git 历史维护**：
    - 完成了对远程 `main` 分支 44 个 commit 的作者信息修复并强制推送。

## 3. 讨论结果
- **Docker 镜像源验证**：测试并确认了多个国内可用镜像源（如 `docker.rainbond.cc`, `hub.rat.dev` 等）的连通性。
- **代理配置方案**：明确了 Docker Desktop 通过 Resources -> Proxies 配置本地 HTTP 代理（127.0.0.1:10809）直连 Docker Hub 的替代路径。

## 4. 待办事项 (TODO)
- [ ] 验证 `docker compose up --build` 在更新后的镜像源下是否能稳定完成。
- [ ] 观察 GitHub 贡献图表是否在 Force Push 后正常恢复。

## 5. 数据源和 API 确认
- 目前讨论集中于基础设施构建，暂无新增 API 变更确认。
