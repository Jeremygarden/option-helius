# 期权平台工作记忆 - 2026-06-08

## 1. 重要决策和方向确认
- **数据源接入策略**：确认为双数据源并行方案。
  - **IBKR Flex Query**：用于获取个人账户的真实持仓、成交历史和行权记录。
  - **yfinance**：作为免费且快速的补充，用于拉取市场期权链数据（IV, Greeks, OI, Vol），支撑策略评分系统。
- **优先级调整**：
  1. P0: 修复后端依赖并接入 yfinance 期权链。
  2. P0: 确保前端容器可用，期权 Tab 优先上线。
  3. P1: 接入 IBKR Flex Query 真实持仓数据。

## 2. 技术实现进展
- **后端 (Backend)**：
  - 骨架已搭建，包含 options, macro, sentiment, picks 等所有 router。
  - 实现了「逃顶危险指数 V2」（18个指标+动态权重）。
  - 确定了 `ibflex` 库作为解析 IBKR Flex XML 的工具。
  - 修复了 `mock/macro.py` 语法错误及 `routers` 中的相对导入问题。
- **前端 (Frontend)**：
  - 多 Tab 页面（chain, macro, sentiment, picks）骨架已建。
  - 完成了 IVSurface3D、GEX 图、OI/Vol 图、KPI 栏及期限结构等核心组件的开发。
- **环境部署**：
  - 使用 Docker Compose 部署（FastAPI + Redis + TimescaleDB + Next.js）。
  - 顺手清理了磁盘空间（释放 6.6G），安装了 Docker 环境。
  - 后端依赖修复：在 `pyproject.toml` 中补齐了 `httpx` 和 `yfinance`。

## 3. 讨论结果和待办事项 (TODO)
- **待办事项**：
  - [ ] **后端**：编写 `services/ibkr_flex.py` 封装 API 调用。
  - [ ] **后端**：对接 `yfinance` 到 `pricing.py`，让前端期权链活起来。
  - [ ] **用户侧**：JJ 需要在 IBKR 端生成 Flex Token 和 Query ID。
  - [ ] **系统**：HEARTBEAT 需包含 Cron 任务健康检查，防止记忆丢失再次发生。
- **讨论结论**：Flex Query 虽然没有实时市场数据，但对分析个人持仓和盈亏完全足够；市场扫描和策略评分则依赖 yfinance。

## 4. 数据源和 API 确认
- **IBKR Flex Web Service**：确认使用 REST API 两步走流程（请求 Reference Code -> 轮询获取 XML）。
- **yfinance**：确认可以免费拉取指定到期日的期权链 DataFrame。
- **环境配置**：需要新建 `backend/config/ibkr.env` 存储敏感 Token。
