# 期权平台工作记忆 - 2026-06-09

## 重要决策和方向确认
- **数据源接入策略**：确立了“双源并行”方案。
    - **P0**: 接入 `yfinance` 期权链，作为核心市场数据（IV/Greeks/OI）的免费、快速实现方案。
    - **P1**: 接入 **IBKR Flex Query**，用于获取真实持仓、成交历史和行权记录。
    - **P2**: 长期考虑接入 IBKR TWS API 以获取实时 Quotes。
- **功能优先级**：
    1. 修复后端依赖并跑通 Docker 环境。
    2. 实现 `yfinance` 对接，让“期权链”Tab 可用。
    3. 完成 IBKR Flex Query 接入，实现真实持仓显示。

## 技术实现进展
- **环境修复**：
    - 在服务器上安装了 Docker & Docker Compose。
    - 清理了磁盘空间（释放 6.6G），解决因磁盘满导致的启动失败。
    - 识别并计划修复后端 `httpx` 和 `yfinance` 依赖缺失问题。
- **系统架构**：
    - 基础设施已搭建：FastAPI + Redis + TimescaleDB + Next.js。
    - 完成了多 Tab 前端骨架及核心组件（IVSurface3D, GEX图, OI/Vol图, KPI栏）。
    - 逃顶危险指数 V2（18指标+动态权重）已在后端运行。

## 待办事项
- [ ] 在 `pyproject.toml` 中补全 `httpx` 和 `yfinance` 依赖。
- [ ] 修复后端容器启动失败问题并重新 Build。
- [ ] 实现 `services/pricing.py` 对接 `yfinance`。
- [ ] 用户（JJ）需在 IBKR 配置 Flex Web Service 并提供 **Token** 和 **Query ID**。
- [ ] 新建 `services/ibkr_flex.py` 处理 Flex Query XML 解析（推荐使用 `ibflex` 库）。

## 数据源和API确认
- **yfinance**：确认可用于抓取指定到期日的期权链数据（`ticker.option_chain(date)`）。
- **IBKR Flex Query**：
    - 确认通过 `FlexStatementService` 接口分两步（请求报告 -> 轮询结果）获取数据。
    - 核心字段：`symbol`, `putCall`, `strike`, `expiry`, `quantity`, `tradePrice`, `fifoPnlRealized`。
- **模型路由**：确认 Cron 任务已修复，统一使用 `github-copilot/gemini-3-flash-preview` 以确保稳定性。
