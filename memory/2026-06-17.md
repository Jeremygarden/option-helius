# Options Dashboard 工作记忆 - 2026-06-17

## 重要决策和方向确认
- **IBKR 集成方案确认**：采用 Sidecar 模式，将 `ib-gateway-docker` 作为独立容器，通过 `ib-async` 协议进行异步通信。
- **数据源升级**：决定接入 IBKR 原生 Model Greeks 和实时 IV，取代 yfinance 的延迟数据，提升 GEX 和 IV Surface 分析精度。
- **开发流程规范**：明确 subagent 运行 ralph 的标准命令，强制要求通过 ralph 调度 openclaw agent 执行修改，确保 commit 记录完整。
- **多 Agent 协作架构**：确立了 Backend (Python)、Infra (Docker)、Reviewer (Quality Control) 三方并行协作的模式。

## 技术实现进展
- **UI 基础优化 (Baseline)**：完成了 8 轮 UI 基础优化，包括 Picks 页面布局重构、全局风格 Polish（JetBrains Mono 字体、深色终端感）。
- **Tailwind/PostCSS 修复**：解决了前端样式“裸奔”问题，补全了 `postcss.config.js` 及其插件配置。
- **IBKR 集成 Phase 1**：
    - Backend: 已加入 `ib-async` 依赖，开始构建基础 client 和 health endpoints。
    - Infra: 完成了 Docker sidecar 配置和环境变量模板。
- **Phase 2 UI 深度打磨**：
    - Chain 页面：完成了 15 轮优化，对齐参考图，重构了 KPIBar、Expiry Tabs 和图表组件。
    - Picks 页面：完成了 15 轮优化，重构了 `StrategyCard` 和详情面板，增加了 Breakeven 分析。

## 讨论结果和待办事项
- **待办**：继续推进 IBKR 集成的后续轮次，确保市场数据流稳健接入。
- **待办**：Reviewer subagent 需持续审查代码质量，修复潜在 bug，并完善集成测试。
- **交互规范**：统一全页面配色（CALL 绿、PUT 红、SPREAD 紫）。

## 数据源和 API 确认
- **主数据源**：确认转向 IBKR (Interactive Brokers) 获取实时市场价格。
- **API 路径**：后端 API 运行在 8000 端口，前端开发服务器运行在 3001 端口（3000 被占用）。
- **Fallbacks**：在后端不可用时，前端具备 Mock 数据 fallback 机制。
