# 期权平台工作记忆 - 2026-06-15

## 1. 重要决策和方向确认
- **数据源多层级回退机制**：确立了以 yfinance 为主，FlashAlpha 为单股 GEX 增强，AlphaVantage 为 Spot Price 兜底的三层架构。
- **项目 Docker 化推广**：确认 option-helius 全面转向 Docker 部署，并为 Windows 用户（Docker Desktop + WSL2）整理了标准化启动流程。
- **任务超时优化**：针对大型自主任务（如 Docker 构建），决策在 Cron Job 中显式设置 `timeoutSeconds: 4800`，并建议重型任务通过对话手动 Spawn 以规避限制。

## 2. 技术实现进展
- **FlashAlpha 接入完成**：
    - 新增 `flashalpha.py` 服务模块。
    - 实现 `get_gex()` 智能路径：单股优先走 FlashAlpha（预计算数据更准），ETF 走 yfinance。
    - 实现 API 配额耗尽自动 Fallback 逻辑。
- **环境隔离优化**：API Key 等敏感信息通过环境变量 `FLASHALPHA_API_KEY` 注入，不再硬编码。
- **状态验证**：Backend 健康检查、策略推荐（8条）、期权链抓取（SPY 266个合约）全部通过实测。

## 3. 讨论结果和待办事项
- **数据源对比结论**：
    - **FlashAlpha**: 单股 GEX 准，但 ETF 需付费。
    - **AlphaVantage**: 免费版仅能获取 Spot Price，期权链需 Premium。
    - **Tradier (待办)**: 下一步计划接入 Tradier Sandbox 账户作为主 Fallback。
- **遗留问题修复**：确认了 `/api/options/chain` 接口 `spot_price` 字段曾出现 None 的现象，已通过代码审计确认字段映射逻辑。

## 4. 数据源和 API 确认
- **FlashAlpha**: Free Tier (5 req/day), API Key 已注入系统。
- **AlphaVantage**: Free Tier (25 req/day), 主要用于 `GLOBAL_QUOTE` 接口。
- **yfinance**: 当前核心免费源，需注意频率限制导致的数据缺失（如 PCR/Spot 返回 Null 的波动）。
