# Options Dashboard Memory - 2026-06-14

## 重要决策和方向确认
- 确定使用多态解析器 (Polymorphic Parser) 或中间件转换来解决 GPT-5.5 模型与 Claude 模型在 `toolCall` 格式上的差异问题。
- 确认系统全链路跑通（Data -> Backend -> Frontend）后，不再重复启动超时的 subagents，而是由主 Agent 接管进行 Smoke Test 和 UI 评审。

## 技术实现进展
- **Backend**: 
  - 修复了 `pyproject.toml` 中的 `httpx` 和 `yfinance` 依赖。
  - `NVDA` 期权链成功从 `yfinance` 抓取真实数据。
  - 实现了缓存机制、期权路由和策略评分器。
  - 接入了 AlphaVantage 作为备用数据源。
- **Frontend**:
  - `/chain` 页面完整重写，包含 3D IV 曲面、GEX 图表、ticker 搜索和到期日选项卡。
  - `/picks` 页面重构，实现生产级策略卡片，包含评分条、Greeks 展开和筛选系统。
  - 修复了 tsconfig 和 recharts 的类型定义。
- **Infrastructure**:
  - Docker 环境稳定运行：backend, frontend, timescaledb, redis 容器状态良好。
  - 解决了 `gpt-5.5` 特有的 `toolCall` 监控不到位的问题。

## 讨论结果和待办事项
- **待办**: 手动重启因内存不足（exit 137）而停止的 Docker 服务。
- **待办**: 针对 A2/C2/D2 中断前完成但未 commit 的改动进行最终合并。
- **策略**: 在后续 Subagent 任务中，第一步先执行 `commit && push` 以防止因超时导致工作丢失。

## 数据源和API确认
- **yfinance**: 主力数据源，用于抓取期权链数据。
- **AlphaVantage**: 备用数据源 (API-KEY: `4ZVBTAERBE2N4AUG`)，用于增强系统鲁棒性。
- **IBKR**: 讨论了接入 IBKR Flex Token 用于持仓数据同步的可能性。
