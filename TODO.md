# 逃顶危险指数 — 未来优化待办

优先级：🔴 高 / 🟠 中 / 🟡 低

---

## 🔴 短期（Phase 2 接入真实数据时同步做）

### T-01: 接入真实数据替换Mock（最高优先级）
权重最高的3个指标目前全是mock，误差最大：
- MOVE → yfinance `^MOVE`
- HY OAS → FRED `BAMLH0A0HYM2`
- 收益率曲线 → FRED `T10Y2Y`
- 金铜比 → yfinance `GC=F` / `HG=F`
- DXY → yfinance `DX-Y.NYB`
- NAAIM → naaim.org 周度爬取

### T-02: 动态regime权重 — 已派subagent实现中 ✅（等待完成）

---

## 🟠 中期

### T-03: 样本质量提升（非美国事件指标值估算→真实值）
当前问题：24个非美事件的18个指标值是人工估算（权重0.7x折扣）
改进方案：
- FRED提供1950年后历史序列（免费）
- Yahoo Finance历史价格序列
- 世界银行历史数据API
目标：将非美事件数据质量从"estimated"升级为"historical"，去掉0.7x折扣

### T-04: 非线性交互项（多指标同时极端的共振效应）
当前模型是线性加权，缺少"多个指标同时极端比单个极端危险得多"的捕捉
```python
bonus = 0.10 × I(MOVE>90 AND HY_OAS>500)    # 债市+信用双杀
bonus += 0.08 × I(AIAE>0.50 AND NAAIM>85)   # 机构+居民双满仓
bonus += 0.06 × I(yield_curve<-30 AND erp<-0.02)  # 利率+估值双压
score_final = min(95, score + bonus)
```

### T-05: Bootstrap由100次升级到1000次
当前Bootstrap只跑了100次（时间限制），统计置信度不足
目标：1000次重采样，CI更稳定
运行时间约3-5分钟，建议单独cron job跑

---

## 🟡 长期

### T-06: 滚动季度优化（自适应权重更新）
每季度末用最新数据重新跑一次4折CV优化，权重随市场结构变化自适应更新
建议：cron job，每季度最后一个交易日触发，结果写入 `analysis/weights_YYYYQN.json`

### T-07: Walk-forward样本外准确率持续追踪
每月新增一个"真实发生"的市场事件数据点，持续追踪模型样本外表现
目标：样本从37扩展到50+，每年追加4-8个数据点

### T-08: Regime-aware模型的回测验证
对37个历史事件，分别判断当时应该用哪个regime的权重，
然后对比：静态权重 vs 动态regime权重 的MSE差异
验证动态权重是否真的更准

### T-09: 前端可视化完善
- 显示各折CV的权重变化（权重稳定性可视化）
- Sigmoid曲线可视化（每个指标当前值在曲线上的位置）
- Regime历史切换时间轴（过去12个月各月的regime状态）

---

## 已完成 ✅

- [x] 8→18指标扩展（2026-05-22）
- [x] Sigmoid连续评分替换step函数（2026-05-22）
- [x] 动量调整层 α=0.15（2026-05-22）
- [x] 样本13→37（全球G7+亚洲事件）（2026-05-22）
- [x] Walk-forward验证 + L2正则化（过拟合比率1.30x）（2026-05-22）
- [x] Bootstrap置信区间100次（2026-05-22）
- [x] 动态regime权重切换（2026-05-22，subagent实现中）
